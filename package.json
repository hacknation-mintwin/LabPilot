{
  "name": "similarity-search",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix onepager run dev",
    "start": "npm --prefix onepager run start"
  },
  "dependencies": {
    "openai": "^4.104.0"
  }
}
